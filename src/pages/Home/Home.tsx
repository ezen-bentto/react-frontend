import Button from "@/components/ui/ButtonEx";

const Home = () => {
  return (
  <div>
    <h1>홈 페이지</h1>
    <Button intent="primary" size="lg">
      버튼
    </Button>
  </div>
  );
};

export default Home;
